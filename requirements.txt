# Core web framework
Flask

# Camera control (requires libgphoto2-dev system package)
gphoto2

# RAW image processing (requires libraw-dev system package)
rawpy

# Image manipulation (preview generation, basic processing)
Pillow

# For video processing (requires ffmpeg system package)
# Using subprocess directly for now, so no specific Python package needed here,
# but ffmpeg command-line tool must be installed on the system.

# Optional: For better background task management later
# Flask-APScheduler
# Celery
